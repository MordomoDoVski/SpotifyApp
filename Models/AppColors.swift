import SwiftUI

struct AppColors {
    // Spotify-inspired color palette
    static let primaryGreen = Color(red: 0.18, green: 0.8, blue: 0.44) // Spotify Green
    static let darkBackground = Color(red: 0.09, green: 0.09, blue: 0.09) // Very dark gray
    static let cardBackground = Color(red: 0.15, green: 0.15, blue: 0.15) // Dark card background
    static let lightText = Color(red: 0.95, green: 0.95, blue: 0.95) // Light white text
    static let secondaryText = Color(red: 0.65, green: 0.65, blue: 0.65) // Gray text
    static let accentGreen = Color(red: 0.22, green: 0.9, blue: 0.5) // Lighter green accent
}
